<div class="modal">
    <div class="thanks-container">
        <h3>Many thanks for signing the petition, please be sure to tell your
            friends about it!</h3>
        <p>There are already {{count}} supporters!</p>
        <p>Interested who has already singed? Click
            <a href="/signers">here</a></p>

        {{#if error}}
            <h2>{{message}}</h2>
        {{/if}}

        <img src={{signature}} alt="copy of users signature" />

        <form method="POST">
            <input type="hidden" name="_csrf" value="{{csrfToken}}" />
            <button>Delete signature</button>
            <p>this deletes your signature for petition data base and takes you
                back to petition main page</p>
        </form>
    </div>
</div>